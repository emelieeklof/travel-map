# Dummy value so App.tsx's "missing API key" screen doesn't shadow the auth-gate
# tests in CI (where no real .env.local exists). Not a real key, never used to
# call any API — these tests never render far enough to load Google Maps.
VITE_GOOGLE_MAPS_API_KEY=test-key-not-real
